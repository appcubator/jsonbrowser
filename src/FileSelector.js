// Generated by CoffeeScript 1.7.1
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  define(function(require, exports, module) {
    var FileSelector, util;
    util = require('util');
    return FileSelector = (function() {
      function FileSelector() {
        this.elementsDroppedTo = __bind(this.elementsDroppedTo, this);
        this.files = [];
        this.currentLevel = null;
      }

      FileSelector.prototype.getIndexOfKey = function(targetKey) {
        var index, key, _i, _len, _ref;
        _ref = this.currentLevel.keys;
        for (index = _i = 0, _len = _ref.length; _i < _len; index = ++_i) {
          key = _ref[index];
          if (key === targetKey) {
            return index;
          }
        }
        return 0;
      };

      FileSelector.prototype.elementClicked = function(key, level) {
        this.currentLevel = level;
        return this.files = [key];
      };

      FileSelector.prototype.elementShiftClicked = function(key, level) {
        var ind, startInd;
        if (level !== this.currentLevel) {
          this.currentLevel = level;
          ind = this.getIndexOfKey(key);
          return this.selectRange(0, ind);
        } else {
          startInd = this.getIndexOfKey(this.files[0]);
          ind = this.getIndexOfKey(key);
          return this.selectRange(startInd, ind);
        }
      };

      FileSelector.prototype.selectRange = function(start, end) {
        var i, key, liView, _i, _j, _k, _len, _len1, _ref, _ref1, _ref2, _ref3, _results;
        if (start > end) {
          _ref = [end, start], start = _ref[0], end = _ref[1];
        }
        this.files = [];
        for (i = _i = start, _ref1 = end + 1; _i < _ref1; i = _i += 1) {
          key = this.currentLevel.keys[i];
          this.files.push(key);
        }
        this.currentLevel.highlightKeys(this.files);
        _ref2 = this.files;
        for (_j = 0, _len = _ref2.length; _j < _len; _j++) {
          key = _ref2[_j];
          liView = this.currentLevel.getLiView(key);
          this.makeDraggable(liView);
        }
        _ref3 = _.difference(this.currentLevel.keys, this.files);
        _results = [];
        for (_k = 0, _len1 = _ref3.length; _k < _len1; _k++) {
          key = _ref3[_k];
          _results.push(this.makeDroppable(this.currentLevel.getLiView(key)));
        }
        return _results;
      };

      FileSelector.prototype.getClonesExceptCurrent = function(currentLiView) {
        var $clones, $domEl, key, liView, p1, p2, _i, _len, _ref;
        $clones = [];
        _ref = this.files;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          key = _ref[_i];
          liView = this.currentLevel.getLiView(key);
          if (liView !== currentLiView) {
            $domEl = $(liView.domEl).clone();
            $domEl.addClass('ui-draggable-dragging');
            $clones.push($domEl);
            p1 = $(liView.domEl).offset();
            p2 = $(currentLiView.domEl).offset();
            $domEl.css({
              marginLeft: p1.left - p2.left,
              marginTop: p1.top - p2.top
            });
          }
        }
        return $clones;
      };

      FileSelector.prototype.makeDraggable = function(liView) {
        var $clones, currentLevel, domEl;
        domEl = liView.domEl;
        $clones = this.getClonesExceptCurrent(liView);
        currentLevel = this.currentLevel;
        return $(domEl).draggable({
          helper: "clone",
          start: function(e, ui) {
            return _.each($clones, function($clone) {
              return $(currentLevel.domEl).append($clone);
            });
          },
          stop: function(e, ui) {
            return _.each($clones, function($clone) {
              return $clone.remove();
            });
          },
          drag: function(e, ui) {
            return _.each($clones, function($clone) {
              return $clone.css({
                position: "absolute",
                top: ui.position.top,
                left: ui.position.left
              });
            });
          }
        });
      };

      FileSelector.prototype.makeDroppable = function(liView) {
        var elementsDroppedTo;
        elementsDroppedTo = this.elementsDroppedTo;
        return $(liView.domEl).droppable({
          drop: function(e, ui) {
            return elementsDroppedTo(liView);
          },
          over: function(e, ui) {
            return $(e.target).addClass('droppable');
          },
          out: function(e, ui) {
            return $(e.target).removeClass('droppable');
          }
        });
      };

      FileSelector.prototype.elementsDroppedTo = function(liView) {
        var key, obj, _i, _len, _ref;
        if (util.getType(this.currentLevel.curObj) === "object") {
          obj = liView.getObj();
          _ref = this.files;
          for (_i = 0, _len = _ref.length; _i < _len; _i++) {
            key = _ref[_i];
            obj[key] = this.currentLevel.curObj[key];
            delete this.currentLevel.curObj[key];
          }
        }
        return this.currentLevel.render(true);
      };

      return FileSelector;

    })();
  });

}).call(this);
